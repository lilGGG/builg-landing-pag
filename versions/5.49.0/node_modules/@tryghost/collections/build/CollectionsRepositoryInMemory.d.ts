import { InMemoryRepository } from '@tryghost/in-memory-repository';
import { Collection } from './Collection';
export declare class CollectionsRepositoryInMemory extends InMemoryRepository<string, Collection> {
    constructor();
    create(data: any): Promise<Collection>;
    protected toPrimitive(entity: Collection): object;
}
